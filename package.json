{
  "name": "{{projectName}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    {{#llsCharts}}
    "llsCharts": "^1.0.6",
    {{/isTools}}
    {{#if llsweb}}
    "llsweb": "^1.4.2"
    {{/if}}
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
